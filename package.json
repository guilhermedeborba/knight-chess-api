{
  "name": "knight-chess-api",
  "version": "1.0.0",
  "description": "Knight Chess API to process in Algebraic notation possible knight moves given the initial position.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "DEBUG=express:router ./node_modules/.bin/nodemon src/server.js --exec babel-node --presets babel-preset-env"
  },
  "author": "Guilherme Borba",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.9.0",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-jest": "^23.6.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-2": "^6.24.1",
    "eslint": "^5.13.0",
    "husky": "^1.3.1",
    "jest": "^24.1.0",
    "lint-staged": "^8.1.4",
    "nodemon": "^1.18.10",
    "prettier": "^1.16.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "eslint --config .eslintrc --fix",
      "prettier --config .prettierrc --write",
      "git add"
    ]
  }
}
